"use strict";(self.webpackChunkcentrifugal_dev=self.webpackChunkcentrifugal_dev||[]).push([[1793],{28453:(e,n,t)=>{t.d(n,{R:()=>r,x:()=>a});var s=t(96540);const o={},i=s.createContext(o);function r(e){const n=s.useContext(i);return s.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),s.createElement(i.Provider,{value:n},e.children)}},55780:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>a,default:()=>h,frontMatter:()=>r,metadata:()=>s,toc:()=>d});const s=JSON.parse('{"id":"tutorial/centrifugo","title":"Integrating Centrifugo for real-time event delivery","description":"Integrate Centrifugo into a chat app with WebSocket connections, JWT authentication, channel subscriptions, and real-time message broadcasting.","source":"@site/docs/tutorial/centrifugo.md","sourceDirName":"tutorial","slug":"/tutorial/centrifugo","permalink":"/docs/tutorial/centrifugo","draft":false,"unlisted":false,"editUrl":"https://github.com/centrifugal/centrifugal.dev/edit/main/docs/tutorial/centrifugo.md","tags":[],"version":"current","frontMatter":{"description":"Integrate Centrifugo into a chat app with WebSocket connections, JWT authentication, channel subscriptions, and real-time message broadcasting.","id":"centrifugo","sidebar_label":"Integrating Centrifugo","title":"Integrating Centrifugo for real-time event delivery"},"sidebar":"Tutorial","previous":{"title":"Creating SPA frontend","permalink":"/docs/tutorial/frontend"},"next":{"title":"Missed messages recovery","permalink":"/docs/tutorial/recovery"}}');var o=t(74848),i=t(28453);const r={description:"Integrate Centrifugo into a chat app with WebSocket connections, JWT authentication, channel subscriptions, and real-time message broadcasting.",id:"centrifugo",sidebar_label:"Integrating Centrifugo",title:"Integrating Centrifugo for real-time event delivery"},a=void 0,c={},d=[{value:"Adding Centrifugo connection",id:"adding-centrifugo-connection",level:2},{value:"Adding JWT connection authentication",id:"adding-jwt-connection-authentication",level:2},{value:"Subscribing on personal channel",id:"subscribing-on-personal-channel",level:2},{value:"Publish real-time messages",id:"publish-real-time-messages",level:2},{value:"Handle real-time messages",id:"handle-real-time-messages",level:2},{value:"Handle message added event",id:"handle-message-added-event",level:2},{value:"Handle user joined event",id:"handle-user-joined-event",level:2},{value:"Handle user left event",id:"handle-user-left-event",level:2},{value:"We did it",id:"we-did-it",level:2}];function l(e){const n={a:"a",code:"code",h2:"h2",li:"li",p:"p",pre:"pre",ul:"ul",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(n.p,{children:"It's finally time for the real-time! In some cases you already have an application and when integrating Centrifugo you start from here."}),"\n",(0,o.jsxs)(n.p,{children:["To add Centrifugo let's update ",(0,o.jsx)(n.code,{children:"docker-compose.yml"})," file:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-yaml",children:"centrifugo:\n  image: centrifugo/centrifugo:v6\n  volumes:\n    - ./centrifugo:/centrifugo\n  command: centrifugo -c config.json\n  expose:\n    - 8000\n"})}),"\n",(0,o.jsxs)(n.p,{children:["And put ",(0,o.jsx)(n.code,{children:"config.json"})," to local ",(0,o.jsx)(n.code,{children:"centrifugo"})," directory with the following content:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-json",children:'{\n  "log_level": "debug",\n  "client": {\n    "token": {\n      "hmac_secret_key": "secret"\n    },\n    "allowed_origins": [\n      "http://localhost:9000"\n    ]\n  },\n  "http_api": {\n    "key": "api_key"\n  },\n  "channel": {\n    "namespaces": [\n      {\n        "name": "personal"\n      }\n    ]\n  }\n}\n'})}),"\n",(0,o.jsxs)(n.p,{children:["We will be using the ",(0,o.jsx)(n.code,{children:"personal"})," ",(0,o.jsx)(n.a,{href:"/docs/server/channels#channel-namespaces",children:"namespace"})," here for user channels. Using separate namespaces for every real-time feature is a recommended approach when working with Centrifugo. Namespaces allow splitting the channel space and configuring behavior separately for different real-time features."]}),"\n",(0,o.jsx)(n.h2,{id:"adding-centrifugo-connection",children:"Adding Centrifugo connection"}),"\n",(0,o.jsx)(n.p,{children:"Our next goal is to connect to Centrifugo from the frontend app. We will do this right after user authenticated and chat layout loaded."}),"\n",(0,o.jsxs)(n.p,{children:["To add the real-time WebSocket connection you need to install Centrifugo's JavaScript SDK \u2013 the package is named ",(0,o.jsx)(n.code,{children:"centrifuge"})," (the project is sometimes referred to as ",(0,o.jsx)(n.code,{children:"centrifuge-js"}),"):"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-bash",children:"npm install centrifuge\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Then import it in ",(0,o.jsx)(n.code,{children:"App.tsx"}),":"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"import { Centrifuge, SubscriptionState } from 'centrifuge';\nimport type { PublicationContext, SubscriptionStateContext, SubscribedContext } from 'centrifuge';\n"})}),"\n",(0,o.jsxs)(n.p,{children:["We also imported some types we will be using in the app (with ",(0,o.jsx)(n.code,{children:"import type"}),", since they are only used in type positions)."]}),"\n",(0,o.jsxs)(n.p,{children:["To establish a connection with Centrifugo as soon as user authenticated in the app we can use ",(0,o.jsx)(n.code,{children:"useEffect"})," React hook with the dependency on ",(0,o.jsx)(n.code,{children:"userInfo"}),":"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"useEffect(() => {\n  if (!userInfo.id) {\n    return;\n  }\n\n  // Create the client synchronously (not inside an async function) so the cleanup below\n  // always has it to disconnect, even if the effect is torn down quickly.\n  const centrifuge = new Centrifuge(WS_ENDPOINT, {\n    debug: true\n  })\n  centrifuge.connect()\n\n  return () => {\n    centrifuge.disconnect()\n  }\n}, [userInfo])\n"})}),"\n",(0,o.jsxs)(n.p,{children:["When user logs out and ",(0,o.jsx)(n.code,{children:"userInfo.id"})," is not set \u2013 the connection to server is closed as we do ",(0,o.jsx)(n.code,{children:"centrifuge.disconnect()"})," in ",(0,o.jsx)(n.code,{children:"useEffect"})," cleanup function. Creating the ",(0,o.jsx)(n.code,{children:"Centrifuge"})," instance directly in the effect body (rather than inside an ",(0,o.jsx)(n.code,{children:"async"})," helper) matters: the cleanup function closes over it, so React can always tear the connection down \u2013 otherwise a quick unmount could leak a connection."]}),"\n",(0,o.jsx)(n.p,{children:"But if you run the code like this \u2013 the connection won't be established. That's bad news! But we also have good news - this means that Centrifugo supports secure communication and we need to authenticate the connection upon establishing it! Let's do this."}),"\n",(0,o.jsx)(n.h2,{id:"adding-jwt-connection-authentication",children:"Adding JWT connection authentication"}),"\n",(0,o.jsxs)(n.p,{children:["Change ",(0,o.jsx)(n.code,{children:"Centrifuge"})," constructor to:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"const centrifuge = new Centrifuge(WS_ENDPOINT, {\n    getToken: getConnectionToken,\n    debug: true\n})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Where ",(0,o.jsx)(n.code,{children:"getConnectionToken"})," is function like this:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-javascript",children:"export const getConnectionToken = async () => {\n  const response = await axios.get(`${API_ENDPOINT_BASE}/api/token/connection/`, {})\n  return response.data.token;\n}\n"})}),"\n",(0,o.jsx)(n.p,{children:"I.e. it makes a request to the backend and receives a connection JWT in response. Again \u2013 the frontend makes a request to the backend to get the Centrifugo connection token. Of course we should implement the view on the backend which processes such requests and generates tokens for authenticated users."}),"\n",(0,o.jsxs)(n.p,{children:["The token must follow specification described in ",(0,o.jsx)(n.a,{href:"/docs/server/authentication",children:"Client JWT authentication"})," chapter. Long story short \u2013 it's just a JWT from ",(0,o.jsx)(n.a,{href:"https://datatracker.ietf.org/doc/html/rfc7519",children:"rfc7519"}),", we can use any JWT library to generate it."]}),"\n",(0,o.jsxs)(n.p,{children:["Let's extend ",(0,o.jsx)(n.code,{children:"backend/app/views.py"})," with this view:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/app/views.py"',children:"import jwt\n\nfrom django.conf import settings\n\n\ndef get_connection_token(request):\n    if not request.user.is_authenticated:\n        return JsonResponse({'detail': 'unauthorized'}, status=401)\n\n    token_claims = {\n        'sub': str(request.user.pk),\n        'exp': int(time.time()) + 120\n    }\n    token = jwt.encode(token_claims, settings.CENTRIFUGO_TOKEN_SECRET)\n\n    return JsonResponse({'token': token})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["\u2013 where ",(0,o.jsx)(n.code,{children:"jwt"})," import is a PyJWT library (",(0,o.jsx)(n.code,{children:"pip install PyJWT"}),"). We generate a JWT where the ",(0,o.jsx)(n.code,{children:"sub"})," claim is set to the current user ID and the token expires in 2 minutes."]}),"\n",(0,o.jsxs)(n.p,{children:["Note, we are using ",(0,o.jsx)(n.code,{children:"settings.CENTRIFUGO_TOKEN_SECRET"})," here, we need to include this option to ",(0,o.jsx)(n.code,{children:"backend/app/settings.py"}),":"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/app/settings.py"',children:"# CENTRIFUGO_TOKEN_SECRET is used to create connection and subscription JWT.\n# SECURITY WARNING: make it strong, keep it in secret, never send to the frontend!\nCENTRIFUGO_TOKEN_SECRET = 'secret'\n"})}),"\n",(0,o.jsxs)(n.p,{children:["It must match the value of the ",(0,o.jsx)(n.code,{children:"client.token.hmac_secret_key"})," option from the Centrifugo configuration."]}),"\n",(0,o.jsxs)(n.p,{children:["Don't forget to include this view in the ",(0,o.jsx)(n.code,{children:"urls.py"})," configuration, and then you can finally connect to Centrifugo from the frontend: upon page load the ",(0,o.jsx)(n.code,{children:"centrifuge-js"})," SDK makes a request to the backend to load the connection token, establishes a WebSocket connection with Centrifugo passing the connection token. Centrifugo validates the token and since the secrets match, Centrifugo can be sure the token contains valid information about the user."]}),"\n",(0,o.jsx)(n.h2,{id:"subscribing-on-personal-channel",children:"Subscribing on personal channel"}),"\n",(0,o.jsx)(n.p,{children:"Awesome! Though simply being connected is not that useful. We want to receive real-time data from Centrifugo. But how will Centrifugo understand how to route published data? Of course, through the channel concept. A client can subscribe to a channel to receive all messages published to that channel."}),"\n",(0,o.jsx)(n.p,{children:"As mentioned before \u2013 for this sort of app using a single individual channel for each user makes a lot of sense."}),"\n",(0,o.jsx)(n.p,{children:"You can ask \u2013 could we simply subscribe to all room channels the current user is a member of? It may be a good thing if you know that users won't have too many groups, let's say 10-100 max. Going above this number will make the UI less efficient. Consider a user who is a member of a thousand groups \u2013 it will require a very heavyweight initial subscribe request. What if the user is a member of 10k groups? So moving all the routing complexity to the backend with a single individual channel on the frontend seems a more reasonable approach for our app. And this will also help us to simplify state recovery later."}),"\n",(0,o.jsxs)(n.p,{children:["We already have namespace ",(0,o.jsx)(n.code,{children:"personal"})," configured in Centrifugo \u2013 so let's use it to construct individual channel for each user."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-javascript",children:"const personalChannel = 'personal:' + userInfo.id\n"})}),"\n",(0,o.jsxs)(n.p,{children:["So for a user with id ",(0,o.jsx)(n.code,{children:"1"})," we will have channel ",(0,o.jsx)(n.code,{children:"personal:1"}),", for user ",(0,o.jsx)(n.code,{children:"2"})," \u2013 ",(0,o.jsx)(n.code,{children:"personal:2"})," \u2013 and so on. Of course in a messenger app we do not want one user to be able to subscribe to a channel belonging to another user. So we will use ",(0,o.jsx)(n.a,{href:"/docs/server/channel_token_auth",children:"subscription token auth"})," for channels here. It's also a JWT loaded from the backend. But this JWT must additionally include ",(0,o.jsx)(n.code,{children:"channel"})," claim. So in React we can create Subscription object this way:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-javascript",children:"export const getSubscriptionToken = async (channel: string) => {\n  const response = await axios.get(`${API_ENDPOINT_BASE}/api/token/subscription/`, {\n    params: { channel: channel }\n  });\n  return response.data.token;\n}\n\nconst sub = centrifuge.newSubscription(personalChannel, {\n    getToken: () => getSubscriptionToken(personalChannel)\n})\nsub.on('publication', (ctx: PublicationContext) => {\n    // Used to process incoming channel publications. We will talk about it soon.\n    onPublication(ctx.data)\n})\n\nsub.subscribe()\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Note that we additionally attach a ",(0,o.jsx)(n.code,{children:"channel"})," URL query parameter when requesting the backend \u2013 so the backend understands which channel to generate the subscription JWT for."]}),"\n",(0,o.jsx)(n.p,{children:"On the backend side we check permission to subscribe and return subscription token:"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/app/views.py"',children:"def get_subscription_token(request):\n    if not request.user.is_authenticated:\n        return JsonResponse({'detail': 'unauthorized'}, status=401)\n\n    channel = request.GET.get('channel')\n    if channel != f'personal:{request.user.pk}':\n        return JsonResponse({'detail': 'permission denied'}, status=403)\n\n    token_claims = {\n        'sub': str(request.user.pk),\n        'exp': int(time.time()) + 300,\n        'channel': channel\n    }\n    token = jwt.encode(token_claims, settings.CENTRIFUGO_TOKEN_SECRET)\n\n    return JsonResponse({'token': token})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Please refer to ",(0,o.jsx)(n.a,{href:"/docs/transports/client_api#subscription-token",children:"client SDK spec"})," for more information about error handling scenarios."]}),"\n",(0,o.jsx)(n.p,{children:"Let's also finish up the logic with real-time subscription status now:"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"sub.on('state', (ctx: SubscriptionStateContext) => {\n  setConnected(ctx.newState === SubscriptionState.Subscribed)\n})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["We keep a simple ",(0,o.jsx)(n.code,{children:"connected"})," boolean in state and render it as a \ud83d\udfe2 / \ud83d\udd34 indicator in the navbar."]}),"\n",(0,o.jsxs)(n.p,{children:["There are several subscription states in all our SDKs - ",(0,o.jsx)(n.code,{children:"unsubscribed"}),", ",(0,o.jsx)(n.code,{children:"subscribing"}),", ",(0,o.jsx)(n.code,{children:"subscribed"}),". You can also listen for them separately for more granular logic and get more detailed information about the reason of subscription loss. See ",(0,o.jsx)(n.a,{href:"/docs/transports/client_api",children:"client SDK spec"})," for more detailed description."]}),"\n",(0,o.jsxs)(n.p,{children:["Now we should be able to connect (and authenticate) and subscribe to a channel (with authorization). Try to open the browser tools network tab and see WebSocket frames exchanged between client and server (we showed how to see this in ",(0,o.jsx)(n.a,{href:"/docs/getting-started/quickstart",children:"quickstart"}),")."]}),"\n",(0,o.jsx)(n.h2,{id:"publish-real-time-messages",children:"Publish real-time messages"}),"\n",(0,o.jsx)(n.p,{children:"Now we have a real-time WebSocket connection which is subscribed to the user's individual channel. It's time to start publishing messages upon changes in chat rooms. In our case, we send a real-time message in one of the following scenarios:"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"someone sends a message to a chat room"}),"\n",(0,o.jsx)(n.li,{children:"user joins a room"}),"\n",(0,o.jsx)(n.li,{children:"user leaves a room"}),"\n"]}),"\n",(0,o.jsxs)(n.p,{children:["But we want all chat room members to receive events. If user ",(0,o.jsx)(n.code,{children:"1"})," sends a messages to chat room, we need to find all current members of this room and publish real-time message to each personal channel. I.e. if three users with IDs ",(0,o.jsx)(n.code,{children:"1"}),", ",(0,o.jsx)(n.code,{children:"2"})," and ",(0,o.jsx)(n.code,{children:"3"})," are members of some room \u2013 then we need to publish message to three channels ",(0,o.jsx)(n.code,{children:"personal:1"}),", ",(0,o.jsx)(n.code,{children:"personal:2"})," and ",(0,o.jsx)(n.code,{children:"personal:3"}),". So all the members will be notified about event in real-time."]}),"\n",(0,o.jsxs)(n.p,{children:["To efficiently publish message to many channels Centrifugo provides ",(0,o.jsx)(n.a,{href:"/docs/server/server_api#broadcast",children:"broadcast"})," API. Let's use HTTP API of Centrifugo:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/chat/views.py"',children:"import requests\n\nfrom django.conf import settings\n\n\nclass CentrifugoMixin:\n    # A helper method to return the list of channels for all current members of specific room.\n    # So that the change in the room may be broadcasted to all the members.\n    def get_room_member_channels(self, room_id):\n        members = RoomMember.objects.filter(room_id=room_id).values_list('user', flat=True)\n        return [f'personal:{user_id}' for user_id in members]\n\n    def broadcast_room(self, room, broadcast_payload):\n        room_id = room.pk\n        room_name = room.name  # used later when we add push notifications\n        # Using Centrifugo HTTP API is the simplest way to send real-time message, and usually\n        # it provides the best latency. The trade-off here is that error here may result in\n        # lost real-time event. Depending on the application requirements this may be fine or not.  \n        def broadcast():\n            session = requests.Session()\n            retries = Retry(total=1, backoff_factor=1, status_forcelist=[500, 502, 503, 504])\n            session.mount('http://', HTTPAdapter(max_retries=retries))\n            try:\n                session.post(\n                    settings.CENTRIFUGO_HTTP_API_ENDPOINT + '/api/broadcast',\n                    data=json.dumps(broadcast_payload),\n                    headers={\n                        'Content-type': 'application/json', \n                        'X-API-Key': settings.CENTRIFUGO_HTTP_API_KEY,\n                        'X-Centrifugo-Error-Mode': 'transport'\n                    }\n                )\n            except requests.exceptions.RequestException as e:\n                logger.error(e)\n\n        # We need to use on_commit here to not send notification to Centrifugo before\n        # changes applied to the database. Since we are inside transaction.atomic block\n        # broadcast will happen only after successful transaction commit.\n        transaction.on_commit(broadcast)\n\n\nclass MessageListCreateAPIView(ListCreateAPIView, CentrifugoMixin):\n    # Same as before\n\n    @transaction.atomic\n    def create(self, request, *args, **kwargs):\n        room_id = self.kwargs['room_id']\n        # Only members of the room may post messages to it.\n        get_object_or_404(RoomMember, user=request.user, room_id=room_id)\n        room = Room.objects.select_for_update().get(id=room_id)\n        room.increment_version()\n        channels = self.get_room_member_channels(room_id)\n        serializer = self.get_serializer(data=request.data)\n        serializer.is_valid(raise_exception=True)\n        obj = serializer.save(room=room, user=request.user)\n        room.last_message = obj\n        room.bumped_at = timezone.now()\n        room.save()\n\n        # This is where we add code to broadcast over Centrifugo API.\n        broadcast_payload = {\n            'channels': channels,\n            'data': {\n                'type': 'message_added',\n                'body': serializer.data\n            },\n            'idempotency_key': f'message_{serializer.data[\"id\"]}'\n        }\n        self.broadcast_room(room, broadcast_payload)\n        \n        headers = self.get_success_headers(serializer.data)\n        return Response(serializer.data, status=status.HTTP_201_CREATED, headers=headers)\n"})}),"\n",(0,o.jsx)(n.p,{children:"Let's mention some important things."}),"\n",(0,o.jsxs)(n.p,{children:["We do broadcasts only after successful commit, using Django's ",(0,o.jsx)(n.code,{children:"transaction.on_commit"})," hook. Otherwise we could get an error on transaction commit - but have already sent a misleading real-time message."]}),"\n",(0,o.jsxs)(n.p,{children:["Here we use ",(0,o.jsx)(n.code,{children:"requests"})," library for making HTTP requests (",(0,o.jsx)(n.code,{children:"pip install requests"}),") and do some retries which is nice to deal with temporary network issues."]}),"\n",(0,o.jsxs)(n.p,{children:["We construct list of channels using ",(0,o.jsx)(n.code,{children:"values_list"})," method of Django queryset to make query more efficient."]}),"\n",(0,o.jsxs)(n.p,{children:["Both ",(0,o.jsx)(n.code,{children:"settings.CENTRIFUGO_HTTP_API_ENDPOINT"})," and ",(0,o.jsx)(n.code,{children:"settings.CENTRIFUGO_HTTP_API_KEY"})," are set in ",(0,o.jsx)(n.code,{children:"settings.py"}),". The endpoint points at the Centrifugo service (the ",(0,o.jsx)(n.code,{children:"centrifugo"})," docker compose service name), and the API key must match the ",(0,o.jsx)(n.code,{children:"http_api.key"})," option from the Centrifugo configuration file:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/app/settings.py"',children:'# Base URL of the Centrifugo HTTP API \u2013 "centrifugo" is the docker compose service name.\nCENTRIFUGO_HTTP_API_ENDPOINT = "http://centrifugo:8000"\n# CENTRIFUGO_HTTP_API_KEY is used for auth in Centrifugo server HTTP API.\n# SECURITY WARNING: make it strong, keep it in secret!\nCENTRIFUGO_HTTP_API_KEY = \'api_key\'\n'})}),"\n",(0,o.jsx)(n.p,{children:"Note the following:"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{children:"'idempotency_key': f'message_{serializer.data[\"id\"]}'\n"})}),"\n",(0,o.jsxs)(n.p,{children:["When publishing we provide ",(0,o.jsx)(n.code,{children:"idempotency_key"})," to Centrifugo \u2013 this allows effectively dropping duplicate publications during configurable time window on Centrifugo side."]}),"\n",(0,o.jsxs)(n.p,{children:["Another important thing is how we designed the data of the real-time event \u2013 note we've included event ",(0,o.jsx)(n.code,{children:"type"})," field on top level. In this case ",(0,o.jsx)(n.code,{children:"message_added"}),". This approach allows easily expanding possible event types \u2013 so the frontend may distinguish between them and process accordingly."]}),"\n",(0,o.jsxs)(n.p,{children:["We can extend ",(0,o.jsx)(n.code,{children:"JoinRoomView"})," and ",(0,o.jsx)(n.code,{children:"LeaveRoomView"})," with similar code to also broadcast room membership events:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-python",metastring:'title="backend/chat/views.py"',children:"class JoinRoomView(APIView, CentrifugoMixin):\n    # Some code skipped here ....\n\n    @transaction.atomic\n    def post(self, request, room_id):\n        # Some code skipped here ....\n        obj, _ = RoomMember.objects.get_or_create(user=request.user, room=room)\n        channels = self.get_room_member_channels(room_id)\n        obj.room.member_count = len(channels)\n        body = RoomMemberSerializer(obj).data\n\n        broadcast_payload = {\n            'channels': channels,\n            'data': {\n                'type': 'user_joined',\n                'body': body\n            },\n            'idempotency_key': f'user_joined_{obj.pk}'\n        }\n        self.broadcast_room(room, broadcast_payload)\n        return Response(body, status=status.HTTP_200_OK)\n\n\nclass LeaveRoomView(APIView, CentrifugoMixin):\n    # Some code skipped here ....\n\n    @transaction.atomic\n    def post(self, request, room_id):\n        # Some code skipped here ....\n        obj = get_object_or_404(RoomMember, user=request.user, room=room)\n        obj.room.member_count = len(channels) - 1\n        pk = obj.pk\n        obj.delete()\n        body = RoomMemberSerializer(obj).data\n\n        broadcast_payload = {\n            'channels': channels,\n            'data': {\n                'type': 'user_left',\n                'body': body\n            },\n            'idempotency_key': f'user_left_{pk}'\n        }\n        self.broadcast_room(room, broadcast_payload)\n        return Response(body, status=status.HTTP_200_OK)\n\n"})}),"\n",(0,o.jsxs)(n.p,{children:["We would also like to mention the concept of room ",(0,o.jsx)(n.code,{children:"version"}),'. Each room has a version field in our app; we increment it by one every time we make some room updates. We then attach the version to every event we publish. This technique may be useful to avoid processing outdated real-time messages on the client side. This is especially useful if we use outbox or CDC techniques where delivery latency increases and the chance to get a real-time message that is no longer current (i.e. the app has already loaded more "fresh" state from the backend) also increases.']}),"\n",(0,o.jsx)(n.h2,{id:"handle-real-time-messages",children:"Handle real-time messages"}),"\n",(0,o.jsxs)(n.p,{children:["As we already shown above the entrypoint for incoming real-time messages on the frontend side is the ",(0,o.jsx)(n.code,{children:"on('publication')"})," callback of the Subscription object:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"sub.on('publication', (ctx: PublicationContext) => {\n    onPublication(ctx.data)\n})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Where ",(0,o.jsx)(n.code,{children:"onPublication"})," simply dispatches to a handler based on the event type:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"const onPublication = (event: RealTimeEvent) => {\n  switch (event.type) {\n    case 'message_added':\n      processMessageAdded(event.body)\n      break\n    case 'user_joined':\n      processUserJoined(event.body)\n      break\n    case 'user_left':\n      processUserLeft(event.body)\n      break\n  }\n}\n"})}),"\n",(0,o.jsxs)(n.p,{children:[(0,o.jsx)(n.code,{children:"RealTimeEvent"})," is a discriminated union of our three event types, declared in ",(0,o.jsx)(n.code,{children:"types.ts"}),". The ",(0,o.jsx)(n.code,{children:"type"})," field lets TypeScript narrow ",(0,o.jsx)(n.code,{children:"body"})," to the right shape in each branch."]}),"\n",(0,o.jsxs)(n.p,{children:["We handle events as they arrive \u2013 there's no need to queue them. This works because our reducer updates are idempotent (we dedupe rooms and messages by id) and every event carries the room ",(0,o.jsx)(n.code,{children:"version"})," we discussed above, so duplicate or out-of-order events are handled safely. The idempotent state design is exactly what keeps this real-time layer simple."]}),"\n",(0,o.jsxs)(n.p,{children:["There is one subtlety though: the subscription is created once (when we connect) and lives for the whole session, so its callback would otherwise close over a stale ",(0,o.jsx)(n.code,{children:"chatState"}),". We keep the latest state in a ref and read ",(0,o.jsx)(n.code,{children:"chatStateRef.current"})," inside the handlers:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"const chatStateRef = useRef(chatState);\nuseEffect(() => {\n  chatStateRef.current = chatState;\n}, [chatState]);\n"})}),"\n",(0,o.jsx)(n.p,{children:"Let's look at each handler."}),"\n",(0,o.jsx)(n.h2,{id:"handle-message-added-event",children:"Handle message added event"}),"\n",(0,o.jsxs)(n.p,{children:["Let's look what's going on inside ",(0,o.jsx)(n.code,{children:"processMessageAdded"})," function:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:'const processMessageAdded = async (body: Message) => {\n  const roomId = body.room.id\n  if (!chatStateRef.current.roomsById[roomId]) {\n    const room = await fetchRoom(String(roomId))\n    if (room) {\n      dispatch({ type: "ADD_ROOMS", payload: { rooms: [room] } })\n    }\n  }\n  if (!chatStateRef.current.messagesByRoomId[roomId]) {\n    // First time we see this room \u2013 load its history (which already includes this message).\n    const messages = await fetchMessages(String(roomId))\n    if (messages) {\n      dispatch({ type: "ADD_MESSAGES", payload: { roomId: roomId, messages: messages } })\n    }\n    return\n  }\n  dispatch({ type: "ADD_MESSAGES", payload: { roomId: roomId, messages: [body] } })\n}\n'})}),"\n",(0,o.jsx)(n.p,{children:"We load the room if it was not loaded yet, and load the room's messages if it's the first time we see a message in the room."}),"\n",(0,o.jsx)(n.h2,{id:"handle-user-joined-event",children:"Handle user joined event"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:'const processUserJoined = async (body: RoomMembership) => {\n  const roomId = body.room.id\n  if (!chatStateRef.current.roomsById[roomId]) {\n    const room = await fetchRoom(String(roomId))\n    if (room) {\n      dispatch({ type: "ADD_ROOMS", payload: { rooms: [room] } })\n    }\n  } else {\n    dispatch({\n      type: "SET_ROOM_MEMBER_COUNT", payload: {\n        roomId: roomId,\n        version: body.room.version,\n        memberCount: body.room.member_count\n      }\n    })\n  }\n}\n'})}),"\n",(0,o.jsx)(n.h2,{id:"handle-user-left-event",children:"Handle user left event"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:'const processUserLeft = async (body: RoomMembership) => {\n  const roomId = body.room.id\n  const room = chatStateRef.current.roomsById[roomId]\n  if (room) {\n    if (body.room.version <= room.version) {\n      return // Outdated event \u2013 we already have a newer version of this room.\n    }\n    if (userInfo.id === body.user.id) {\n      dispatch({ type: "DELETE_ROOM", payload: { roomId: roomId } })\n    } else {\n      dispatch({\n        type: "SET_ROOM_MEMBER_COUNT", payload: {\n          roomId: roomId,\n          version: body.room.version,\n          memberCount: body.room.member_count\n        }\n      })\n    }\n  } else if (userInfo.id !== body.user.id) {\n    const room = await fetchRoom(String(roomId))\n    if (room) {\n      dispatch({ type: "ADD_ROOMS", payload: { rooms: [room] } })\n    }\n  }\n}\n'})}),"\n",(0,o.jsxs)(n.p,{children:["Here the room ",(0,o.jsx)(n.code,{children:"version"})," check shows its value: if we receive a ",(0,o.jsx)(n.code,{children:"user_left"})," event that is older than the room state we already have (for example a late event arriving after we loaded fresher data), we simply ignore it."]}),"\n",(0,o.jsx)(n.h2,{id:"we-did-it",children:"We did it"}),"\n",(0,o.jsx)(n.p,{children:"Awesome \u2013 we now have an application with real-time features powered by Centrifugo! Messages and room membership changes are now delivered to users in real-time. Though, it's not the end of our journey. So please, take a break \u2013 and then proceed to the next part."})]})}function h(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,o.jsx)(n,{...e,children:(0,o.jsx)(l,{...e})}):l(e)}}}]);